# API (Python)

- `SimulationConfig`: control sample size, means, variance.
- `simulate_block(cfg) -> DataFrame`: produces cultural inputs and linguistic outcomes.
- `compute_scores(df) -> DataFrame`: adds `NLIS` and `CRM` composite scores.
