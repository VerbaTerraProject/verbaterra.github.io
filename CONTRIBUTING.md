# Contributing to VerbaTerra SimLab

- Use **Conventional Commits** (`feat:`, `fix:`, `chore:`, `docs:`, `refactor:`).
- Run `pre-commit` before pushing.
- Add/modify tests for changes.
- Keep public APIs typed and documented.
