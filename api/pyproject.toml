[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "verbaterra-api"
version = "0.1.0"
dependencies = ["fastapi>=0.115", "uvicorn>=0.30", "pydantic>=2.0", "numpy>=1.24", "pandas>=2.0"]

